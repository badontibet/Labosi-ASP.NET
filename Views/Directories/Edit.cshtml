@model DirectoryItemFormViewModel

@{
    ViewData["Title"] = "Edit Directory " + Model.Name;
}

<nav class="breadcrumb" aria-label="Breadcrumb">
    <a asp-controller="Home" asp-action="Index">Dashboard</a>
    <span>/</span>
    <a asp-controller="Directories" asp-action="Index">Directories</a>
    <span>/</span>
    <span>Edit @Model.Name</span>
</nav>

<section class="page-hero">
    <span class="eyebrow">Directory metadata</span>
    <h1>Edit Directory</h1>
    <p>Update directory metadata without changing file records in this directory.</p>
</section>

<section class="panel form-panel">
    <form asp-controller="Directories" asp-action="Edit" asp-route-id="@Model.Id" method="post" novalidate data-lab4-form>
        @Html.AntiForgeryToken()
        <input asp-for="Id" type="hidden" />
        <partial name="_DirectoryForm" model="Model" />

        <div class="form-actions">
            <button type="submit" class="primary-action">Save changes</button>
            <a class="link-chip" asp-controller="Directories" asp-action="Details" asp-route-id="@Model.Id">Cancel</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
